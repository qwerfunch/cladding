# Conformance fixtures contain *synthetic* secret-shaped strings on
# purpose (so stage_1.6 fail fixtures actually fail). They never reach
# production code paths, so secretlint should skip the runner file.
conformance/**
