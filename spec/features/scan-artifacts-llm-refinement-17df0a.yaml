id: F-17df0a
title: docs/conventions.md + spec/architecture.yaml LLM refinement (v0.3.35)
status: done
modules:
  - src/cli/scan/llm.ts
  - src/cli/scan/index.ts
  - src/cli/init.ts
depends_on:
  - F-7fa4a7
capability_refs: []
acceptance_criteria:
  - id: AC-001
    statement: The system shall route the deep scan artifacts through the dispatcher
      chain so docs/conventions.md and spec/architecture.yaml inherit the same
      LLM refinement that docs/project-context.md already enjoys.
  - id: AC-002
    statement: While the dispatcher returns a reply that parses to an empty
      conventions or architecture section, the system shall collapse to the
      deterministic interpreter so the resulting artifacts always carry real
      observed data.
  - id: AC-003
    statement: While the dispatcher throws or selectDispatcher returns null, the
      system shall deliver the deterministic artifacts unchanged from v0.3.34 so
      the no-LLM majority of clad init invocations sees no behavioural change.
  - id: AC-004
    statement: The system shall select the dispatcher once at the top of runInit so
      both refinement paths share a single host session and the noLlm override
      evaluates exactly once.
    evidence_refs:
      - src/cli/init.ts
