id: F-0f4dd6
slug: natural-language-init
title: "Natural-language init across skill and MCP hosts"
status: in_progress
modules:
  - src/serve/server.ts
  - src/cli/init.ts
  - src/cli/clarify.ts
  - src/cli/host-onboarding.ts
  - src/cli/hook.ts
  - src/ui/softShell.ts
  - src/init/host-setup.ts
  - src/init/agents-md.ts
  - src/init/host-instructions.ts
  - src/spec/new.ts
  - src/spec/schema.json
  - src/spec/types.ts
  - src/cli/done.ts
  - src/events/log.ts
  - README.md
  - docs/glossary.md
depends_on: [F-56abaa, F-5f6b45, F-80d19d]
acceptance_criteria:
  - id: AC-001
    ears: ubiquitous
    action: expose project initialization and onboarding answers as MCP tools that reuse the CLI core
    response: MCP hosts can complete init and its follow-up Q&A without asking the user to run a shell command
    text: The system shall expose project initialization and onboarding answers as MCP tools that reuse the CLI core, so MCP hosts can complete init and its follow-up Q&A without asking the user to run a shell command.
    test_refs: [tests/serve/server.test.ts, tests/serve/init-tools.test.ts]
  - id: AC-002
    ears: event
    condition: when a new-project initialization request omits the project idea
    action: return a needs-input result before creating any project artifact
    response: the host asks for intent instead of producing a generic scaffold
    text: When a new-project initialization request omits the project idea, the system shall return a needs-input result before creating any project artifact, so the host asks for intent instead of producing a generic scaffold.
    test_refs: [tests/serve/init-tools.test.ts]
  - id: AC-003
    ears: event
    condition: when initialization uses a planning document
    action: read a recognized UTF-8 text file confined to the connected project root and pass its complete body through the existing intent-aware onboarding path
    response: the planning document is neither truncated nor mistaken for literal intent
    text: When initialization uses a planning document, the system shall read a recognized UTF-8 text file confined to the connected project root and pass its complete body through the existing intent-aware onboarding path, so the planning document is neither truncated nor mistaken for literal intent.
    test_refs: [tests/serve/init-tools.test.ts]
  - id: AC-004
    ears: event
    condition: when initialization adopts an existing project
    action: force the observed scan path even when the project contains fewer than three source files
    response: sparse existing projects are not treated as greenfield ideas
    text: When initialization adopts an existing project, the system shall force the observed scan path even when the project contains fewer than three source files, so sparse existing projects are not treated as greenfield ideas.
    test_refs: [tests/serve/init-tools.test.ts]
  - id: AC-005
    ears: unwanted
    condition: if the connected project already contains spec.yaml and refresh was not explicitly requested
    action: return an already-initialized result without creating proposals or changing project files
    response: repeated natural-language requests remain non-destructive
    text: If the connected project already contains spec.yaml and refresh was not explicitly requested, the system shall return an already-initialized result without creating proposals or changing project files, so repeated natural-language requests remain non-destructive.
    test_refs: [tests/serve/init-tools.test.ts]
  - id: AC-006
    ears: event
    condition: when init or clarify returns pending product questions
    action: include the next question and onboarding status in structured MCP output
    response: the host can continue one answer at a time until onboarding is complete
    text: When init or clarify returns pending product questions, the system shall include the next question and onboarding status in structured MCP output, so the host can continue one answer at a time until onboarding is complete.
    test_refs: [tests/serve/init-tools.test.ts]
  - id: AC-007
    ears: state
    condition: while clad setup wires host integrations
    action: leave the current project tree unchanged and direct the user to open a project and request Cladding in natural language
    response: global host setup never implies project adoption
    text: While clad setup wires host integrations, the system shall leave the current project tree unchanged and direct the user to open a project and request Cladding in natural language, so global host setup never implies project adoption.
    test_refs: [tests/cli/setup.test.ts]
  - id: AC-008
    ears: ubiquitous
    action: document natural-language init as the primary path and describe each host's optional explicit invocation syntax accurately
    response: Claude and Gemini use their slash command, Codex uses its Cladding skill picker, and MCP hosts use natural language
    text: The system shall document natural-language init as the primary path and describe each host's optional explicit invocation syntax accurately, so Claude and Gemini use their slash command, Codex uses its Cladding skill picker, and MCP hosts use natural language.
    evidence_refs: [README.md, docs/setup.md]
    notes: |
      ## Decision
      Keep raw `clad init` as a compatibility and automation surface, not the primary onboarding UX.
      ## Why
      A host-neutral natural-language entry point preserves the product model while host-specific syntax remains an optional accelerator.
      ## Trade-off
      MCP init and clarify tools are required to make that promise deterministic for hosts without a bundled skill.
  - id: AC-009
    ears: unwanted
    condition: if an initialization request has not been followed by a separate affirmative user confirmation after the read-only preview
    action: reject the apply step without creating project artifacts
    response: opening a project, asking about Cladding, or making the initial natural-language request cannot silently authorize file changes
    text: If an initialization request has not been followed by a separate affirmative user confirmation after the read-only preview, the system shall reject the apply step without creating project artifacts, so opening a project, asking about Cladding, or making the initial natural-language request cannot silently authorize file changes.
    test_refs: [tests/serve/init-tools.test.ts]
    notes: |
      ## Decision
      Natural language may start the read-only preparation, but a distinct user turn authorizes the write.
      ## Why
      All supported MCP hosts can call tools, but they do not expose a common cryptographic user-consent primitive.
      ## Trade-off
      The host instruction and verbatim confirmation field provide the strongest portable boundary; a malicious host could still fabricate consent, so this is not presented as a sandbox guarantee.
  - id: AC-010
    ears: unwanted
    condition: if the apply request does not carry the exact one-time approval challenge shown in the read-only preview
    action: reject initialization without writing project artifacts
    response: questions, acknowledgements, and model-invented confirmations cannot be mistaken for user consent
    text: If the apply request does not carry the exact one-time approval challenge shown in the read-only preview, the system shall reject initialization without writing project artifacts, so questions, acknowledgements, and model-invented confirmations cannot be mistaken for user consent.
    test_refs: [tests/serve/init-tools.test.ts]
    notes: |
      ## Decision
      Use a short preview-bound approval challenge rather than attempting to classify arbitrary affirmative prose across languages.
      ## Why
      MCP has no portable proof of which user turn produced an argument; exact challenge matching is the strongest host-neutral check available and makes accidental application materially harder.
      ## Trade-off
      Initialization is a one-time destructive boundary, so one explicit copy/confirm step is preferred over a lower-friction but ambiguous yes/no classifier.
  - id: AC-011
    ears: event
    condition: when the supplied idea, planning document, and observed project already resolve the important product decisions
    action: emit no follow-up questions
    response: complete inputs can move directly into development while incomplete inputs receive at most three material product questions
    text: When the supplied idea, planning document, and observed project already resolve the important product decisions, the system shall emit no follow-up questions, so complete inputs can move directly into development while incomplete inputs receive at most three material product questions.
    test_refs: [tests/serve/init-tools.test.ts, tests/cli/intent-onboarding.test.ts]
  - id: AC-012
    ears: event
    condition: when a planning document and existing source code are both present
    action: combine the complete project-local document with an observed code scan in one onboarding preparation
    response: hybrid projects do not have to discard either declared intent or sparse implementation evidence
    text: When a planning document and existing source code are both present, the system shall combine the complete project-local document with an observed code scan in one onboarding preparation, so hybrid projects do not have to discard either declared intent or sparse implementation evidence.
    test_refs: [tests/serve/init-tools.test.ts]
  - id: AC-013
    ears: event
    condition: when a follow-up answer refines untouched Cladding-generated onboarding artifacts
    action: update those artifacts atomically and keep onboarding active until every answer is represented in the active design
    response: onboarding cannot report completion while the accepted answer exists only in a review proposal
    text: When a follow-up answer refines untouched Cladding-generated onboarding artifacts, the system shall update those artifacts atomically and keep onboarding active until every answer is represented in the active design, so onboarding cannot report completion while the accepted answer exists only in a review proposal.
    test_refs: [tests/serve/init-tools.test.ts, tests/cli/clarify.test.ts]
  - id: AC-014
    ears: unwanted
    condition: if an onboarding artifact changed after the read-only preparation or a write fails while applying the draft
    action: leave every authored project file at its pre-apply content and return a stale or failed result
    response: initialization and clarification never leave a partially applied design
    text: If an onboarding artifact changed after the read-only preparation or a write fails while applying the draft, the system shall leave every authored project file at its pre-apply content and return a stale or failed result, so initialization and clarification never leave a partially applied design.
    test_refs: [tests/serve/init-tools.test.ts]
  - id: AC-015
    ears: event
    condition: when onboarding completes
    action: report that ordinary natural-language development may continue, distinguish on-demand checks from opt-in hook or CI enforcement, and require each material feature to resolve its design impact before done
    response: the handoff is truthful and Tier-B design grows selectively with the project
    text: When onboarding completes, the system shall report that ordinary natural-language development may continue, distinguish on-demand checks from opt-in hook or CI enforcement, and require each material feature to resolve its design impact before done, so the handoff is truthful and Tier-B design grows selectively with the project.
    test_refs: [tests/serve/server.test.ts, tests/cli/done.test.ts]
    evidence_refs: [README.md, src/init/agents-md.ts, src/serve/server.ts]
  - id: AC-016
    ears: event
    condition: when a host restarts its MCP server between the read-only preview and the user's separate approval reply
    action: validate a restart-safe approval envelope against the current workspace snapshot
    response: headless and process-per-turn hosts can apply the approved preview while stale or replayed envelopes remain non-destructive
    text: When a host restarts its MCP server between the read-only preview and the user's separate approval reply, the system shall validate a restart-safe approval envelope against the current workspace snapshot, so headless and process-per-turn hosts can apply the approved preview while stale or replayed envelopes remain non-destructive.
    test_refs: [tests/serve/init-tools.test.ts]
  - id: AC-017
    ears: state
    condition: while the connected project has no spec.yaml and the user has not explicitly named Cladding
    action: keep every non-init Cladding skill ineligible for ordinary development requests
    response: globally installed integrations do not route unrelated work through Cladding personas
    text: While the connected project has no spec.yaml and the user has not explicitly named Cladding, the system shall keep every non-init Cladding skill ineligible for ordinary development requests, so globally installed integrations do not route unrelated work through Cladding personas.
    test_refs: [tests/init/skill-activation.test.ts]
