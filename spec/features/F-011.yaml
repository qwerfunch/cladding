id: F-011
title: MISSING_IMPLEMENTATION drift detector (#2)
status: done
modules:
  - src/stages/detectors/missing-implementation.ts
depends_on:
  - F-003
  - F-010
capability_refs: []
acceptance_criteria:
  - id: AC-017
    statement: The system shall emit an error-severity drift finding for every
      module path declared in spec.yaml that does not exist on disk, failing
      stage_1.3 when at least one is produced.
    evidence_refs:
      - fixture:missing-implementation
  - id: AC-018
    statement: If spec.yaml is absent or unparseable, then the detector shall emit
      one info finding rather than a blocking error of its own while
      ABSENCE_OF_GOVERNANCE remains the authoritative blocking signal so the
      stage fails exactly once for the governance gap instead of once per
      detector.
    kind: behavior
    evidence_refs:
      - fixture:F-011_AC-018
