id: F-9d168287
title: EARS complex pattern — first-class while+when (precondition+trigger),
  completing the 6th canonical EARS shape
status: done
modules:
  - src/spec/types.ts
  - src/spec/ears.ts
  - src/spec/new.ts
  - src/serve/server.ts
  - src/spec/schema.json
  - src/stages/detectors/spec-conformance.ts
depends_on: []
capability_refs: []
acceptance_criteria:
  - id: AC-76572b2d
    statement: The system shall recognize 'complex' as a first-class EARS pattern in
      every enum mirror.
  - id: AC-39ede515
    statement: When an AC is ears='complex', the system shall require both a
      while-precondition and a when-trigger.
  - id: AC-7c4353a2
    statement: If a complex condition omits either clause, then the system shall
      emit a shape issue naming the missing clause.
  - id: AC-d50b6082
    statement: While validating the original five patterns, the system shall behave
      exactly as before (backward compatible).
