id: F-051
title: Detector severity policy + opt-in strict mode
status: done
modules:
  - src/stages/drift.ts
  - src/cli/clad.ts
  - src/stages/detectors/README.md
  - README.md
  - README.ko.md
depends_on:
  - F-022
capability_refs: []
acceptance_criteria:
  - id: AC-101
    statement: The system shall document every detector's severity and its gating
      condition in src/stages/detectors/README.md.
    evidence_refs:
      - src/stages/detectors/README.md
  - id: AC-102
    statement: The system shall derive public detector-set claims from the detector
      catalog during documentation generation or validation without encoding a
      historical detector partition in feature prose.
    kind: quality
    notes: >
      ## Historical schema-0.1 note

      The “3 always-error + 16 conditional” partition is historical, not current
      proof.
  - id: AC-103
    statement: The clad check command shall accept a --strict flag that promotes
      warn-severity drift findings to stage failures.
    kind: behavior
    evidence_refs:
      - src/cli/clad.ts
  - id: AC-104
    statement: While runDrift is called with strict=true, the system shall return
      pass=false when any warn finding exists.
  - id: AC-105
    statement: The severity matrix shall cite the 2026-05-19 drift-inject
      measurement as its evidence source.
    evidence_refs:
      - src/stages/detectors/README.md
