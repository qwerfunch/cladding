id: F-010
title: spec.yaml schema + parser + validator
status: done
modules:
  - src/spec/types.ts
  - src/spec/schema.json
  - src/spec/parse.ts
  - src/spec/validate.ts
  - src/spec/cli.ts
  - src/spec/load.ts
depends_on: []
capability_refs: []
acceptance_criteria:
  - id: AC-015
    statement: The system shall parse spec.yaml and validate it against schema.json,
      returning point-by-point errors when invalid.
    evidence_refs:
      - self-dogfood:spec:validate
  - id: AC-016
    statement: While the spec.schema field matches the supported version, the system
      shall accept the document as input for downstream drift detectors.
    evidence_refs:
      - fixture:F-010_AC-016
