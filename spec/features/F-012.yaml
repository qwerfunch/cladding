id: F-012
title: UNMAPPED_ARTIFACT drift detector (#1)
status: done
modules:
  - src/stages/detectors/unmapped-artifact.ts
depends_on:
  - F-003
  - F-010
capability_refs: []
acceptance_criteria:
  - id: AC-019
    statement: The system shall emit an error-severity drift finding for every
      source file under the scanned patterns that no feature in spec.yaml
      claims, failing stage_1.3 when at least one is produced.
    evidence_refs:
      - self-dogfood:stage:drift
    notes: Scan patterns intentionally narrow (cladding's own tsconfig.include) so
      test fixtures and tooling configs do not surface as false positives.
  - id: AC-020
    statement: If spec.yaml is absent or unparseable, then the detector shall emit
      one info finding rather than a blocking error of its own while
      ABSENCE_OF_GOVERNANCE remains the authoritative blocking signal so the
      stage fails exactly once for the governance gap instead of once per
      detector.
    kind: behavior
    evidence_refs:
      - fixture:F-012_AC-020
