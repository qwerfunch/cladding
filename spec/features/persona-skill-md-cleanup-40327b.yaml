id: F-40327b
slug: persona-skill-md-cleanup
title: persona + skill md drift cleanup
status: done
modules:
  - src/agents/developer.md
  - src/agents/planner.md
  - src/agents/orchestrator.md
  - src/agents/reviewer.md
  - src/agents/observability.md
  - src/agents/README.md
  - skills/init/SKILL.md
  - skills/run/SKILL.md
  - skills/status/SKILL.md
  - skills/check/SKILL.md
  - skills/doctor/SKILL.md
  - skills/checkpoint/SKILL.md
  - skills/rollback/SKILL.md
  - skills/route/SKILL.md
  - scripts/build-plugin.mjs
  - scripts/plugin-mirror-policy.mjs
  - src/assurance/criterion-observations.ts
  - src/assurance/kernel.ts
  - src/assurance/adapters.ts
  - src/assurance/workspace.ts
  - src/spec/compiler/artifact-registry.ts
depends_on:
  - F-bb15e6
acceptance_criteria:
  - id: AC-001
    ears: ubiquitous
    action: realign every persona canonical prompt under src/agents/ with current canonical guidance so a consumer project installing the cladding plugin receives accurate guidance
    response: 'src/agents/developer.md swaps the docs/code-style.md reference for the consumer-facing docs/conventions.md auto-generated by clad init --scan with a fallback note for cladding-self; src/agents/planner.md replaces the F-NNN-only language with explicit F-<hash6> guidance for new features and adds a Scenarios policy paragraph documenting auto-extraction deprecation; src/agents/orchestrator.md adds an LLM-dispatcher-health row to the routing table that points at observability via clad doctor and replaces the F-049 example with a hash example; src/agents/reviewer.md drops the broken ironclad-design/13 cross-link from the frontmatter description and the guardrails table; src/agents/observability.md adds .cladding/events.log.jsonl to the Sources table and adds a Sentinel-miss summary entry to the Reports list pointing at clad doctor; src/agents/README.md retires the broken ironclad-design/14 cross-link with a self-contained CLAIM and moves CONVENTION_DRIFT and UNTESTED_AC out of [TBD] into a new [DONE_SINCE_T9] section; the persistent ironclad-design §1.2 Soft Shell cross-link is removed from every canonical persona because the inline guidance already names the src/ui/softShell.ts helpers'
    text: The system shall realign every persona canonical prompt under src/agents/ with current canonical guidance so a consumer project installing the cladding plugin receives accurate guidance.
    evidence_refs: [src/agents/developer.md, src/agents/planner.md, src/agents/orchestrator.md, src/agents/reviewer.md, src/agents/observability.md, src/agents/README.md]
  - id: AC-002
    ears: ubiquitous
    action: bring the four existing skills/<verb>/SKILL.md surfaces back to parity with the current clad CLI
    response: 'skills/init/SKILL.md grows the --scan, --no-llm, and --roots flag entries, documents the scan artifacts (docs/conventions.md, spec/architecture.yaml, spec/capabilities.yaml, docs/project-context.md, spec/scenarios/README.md), and clarifies that new features use F-<hash6> with a one-liner generator command; skills/run/SKILL.md adds the --cwd <path> flag and a post-run clad doctor cross-reference; skills/status/SKILL.md disambiguates legacy F-NNN from F-<hash6> in the rows description; skills/check/SKILL.md notes that the Drift stage runs every active detector under src/stages/detectors/ with the filesystem-derived live count and build-plugin recount mechanism'
    text: The system shall bring the four existing skills/<verb>/SKILL.md surfaces back to parity with the current clad CLI so adopters do not discover undocumented flags by reading the binary's --help output.
    evidence_refs: [skills/init/SKILL.md, skills/run/SKILL.md, skills/status/SKILL.md, skills/check/SKILL.md]
  - id: AC-003
    ears: ubiquitous
    action: declare the current canonical SKILL.md manifest so plugin projections cannot retain removed or undeclared entries
    response: 'the code-owned mirror policy names every current canonical skill input and fails closed on a missing, malformed, extra, or colliding entry; provider projections are derived only from that manifest'
    text: The system shall declare the current canonical SKILL.md manifest so plugin projections cannot retain removed or undeclared entries.
    evidence_refs: [skills/, scripts/plugin-mirror-policy.mjs]
  - id: AC-004
    ears: event
    condition: "when the plugin mirror build runs"
    response: "derive and reconcile the complete managed persona and skill byte map"
    text: "When the plugin mirror build runs, it shall derive every managed persona and skill mirror from its canonical src/agents/ or skills/ input, reject a missing/extra/stale generated destination, and produce no diff on a second run."
    test_refs:
      - "tests/scripts/plugin-mirror-policy.test.ts#[covers:F-40327b/AC-004] derives every persona/skill lane, rejects missing/extra/stale bytes, and has an idempotent byte map"
    notes: "The canonical manifest names six persona briefs and thirteen skills; README is not a persona.\n\n## Non-normative history (2026-08-31)\n\nReplaced fixed counts, historical Gemini fan-out, and byte-equality prose with the shared pure mirror-census policy."
  - id: AC-005
    ears: state
    condition: while the canonical persona sources reference the inline src/ui/softShell.ts helpers
    action: remove the broken ironclad-design/03-ux-routing.md §1.2 cross-link from every persona because the helper names already convey the guidance
    response: 'every canonical persona (blind-author / developer / observability / orchestrator / planner / reviewer) drops the trailing "See ironclad-design/03-ux-routing.md §1.2" sentence; the Soft Shell paragraph keeps its substantive guidance (use featureLabel / gateLabel / haltMessage from src/ui/softShell.ts) so no behavioural advice is lost; a grep for "ironclad-design" across src/agents/ and skills/ returns zero results post-edit so a future build cannot silently re-introduce the broken path'
    text: While the canonical persona sources reference the inline src/ui/softShell.ts helpers, the system shall remove the broken ironclad-design/03-ux-routing.md §1.2 cross-link from every persona because the helper names already convey the guidance.
    evidence_refs: [src/agents/]
  - id: AC-006
    ears: ubiquitous
    action: verify the post-edit state through the standard cladding gates so the doc cleanup ships with the same quality bar as a code cycle
    response: 'the relevant current test, lint/type, plugin-mirror, specification-validation, and strict-gate checks are run under project policy; no historical test, verb, persona, feature, or detector count is an acceptance condition; manual greps for ironclad-design, docs/code-style, and F-049 across src/agents/ + skills/ return empty results'
    text: The system shall verify the post-edit state through the standard cladding gates so the doc cleanup ships with the same quality bar as a code cycle.
    evidence_refs: [tests/cli/clad.test.ts, scripts/build-plugin.mjs]
