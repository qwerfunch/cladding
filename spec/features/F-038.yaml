id: F-038
title: CONVENTION_DRIFT drift detector (#6, v0.1 floor)
status: done
modules:
  - src/stages/detectors/convention-drift.ts
depends_on:
  - F-003
  - F-010
acceptance_criteria:
  - id: AC-058
    ears: event
    condition: when a TS module under features[].modules has no file-header comment
    action: emit a warn finding referencing the Why>What guardrail
    response: stage_1.3 surfaces but does not fail
    text: When a TypeScript module under features[].modules has no file-header
      comment, the detector shall emit a warn finding referencing the Why>What
      guardrail.
    notes: v0.1 floor. The richer LLM-assisted semantic variant lives behind the
      reviewer agent in v0.2+.
    evidence_refs: [fixture:F-038_AC-058]
