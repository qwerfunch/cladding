id: F-1e9ef827
title: Measure family extraction — clad.ts sheds ~150 lines with byte-identical output
status: done
modules:
  - src/cli/measure.ts
  - src/cli/clad.ts
depends_on: []
capability_refs: []
acceptance_criteria:
  - id: AC-30b84594
    statement: The measure family's internal helpers (sessions renderer, adoption
      section renderer, trend renderer) shall live in src/cli/measure.ts, with
      runMeasureCommand remaining in clad.ts as a thin wrapper — the light
      variant that requires zero spec-shard module edits.
    notes: >-
      ## Why

      clad.ts is the repo's largest file (1,318 lines) and ~40 shards bind it as
      a module, so MOVING exported command surfaces is systematically heavy; the
      measure internals are the one cohesive ~150-line block extractable with no
      shard ripple (2026-07-05 audit).
  - id: AC-815591d4
    statement: The measure command shall render its default, --sessions, --sessions
      --json, and --trend invocations byte-identically to pre-extraction
      behavior while the existing measure and adoption test suites remain green
      with zero test edits.
    kind: quality
