id: F-048
title: Cladding drive — deterministic L1 floor for autonomous loops
status: done
modules:
  - src/drive/halt.ts
  - src/drive/loop.ts
  - src/cli/clad.ts
depends_on:
  - F-031
  - F-047
acceptance_criteria:
  - id: AC-083
    ears: ubiquitous
    action: ship a deterministic autonomous loop that scaffolds features without invoking an LLM
    response: src/drive/loop.ts iterates ready features in dependency order, creates module stubs on disk, runs L1 gates (type · lint · arch), and records evidence per pass
    text: >-
      The system shall ship a deterministic drive floor — an autonomous
      loop that scaffolds in_progress and planned features without
      invoking an LLM. The loop iterates ready features in dependency
      order, materialises module stubs, and runs L1 gates (type · lint
      · arch), recording an evidence entry per successful pass.
    test_refs:
      - tests/drive/halt.test.ts
  - id: AC-084
    ears: event
    condition: when the run loop terminates
    action: return a HaltReason with a class declared by the current HaltClass registry
    response: the detail identifies the deciding condition
    text: When the run loop terminates, it shall return a HaltReason whose class is declared by the current HaltClass registry and whose detail identifies the deciding condition.
    test_refs:
      - "tests/drive/loop.test.ts#[covers:F-048/AC-084] returns a declared halt class with a deciding detail"
    notes: |
      ## Historical note
      The historical closed ten-class enumeration is obsolete because transport classes expanded.
