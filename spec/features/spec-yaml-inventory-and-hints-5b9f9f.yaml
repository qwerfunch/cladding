id: F-5b9f9f
title: spec.yaml gains inventory (auto-maintained shard counts) + ai_hints
  (agent behavior guidance) (v0.3.56)
status: done
modules:
  - src/spec/schema.json
  - src/spec/types.ts
  - src/spec/inventory.ts
  - src/cli/clad.ts
  - tests/spec/inventory.test.ts
  - spec.yaml
depends_on:
  - F-3a5339
capability_refs: []
acceptance_criteria:
  - id: AC-001
    statement: The system shall extend the Spec schema with optional inventory and
      ai_hints blocks.
  - id: AC-002
    statement: The system shall ship inventory compute and write helpers that
      preserve spec.yaml comments and ordering.
  - id: AC-003
    statement: The system shall make clad sync auto-rewrite the inventory block on
      every invocation.
  - id: AC-004
    statement: The system shall dogfood ai_hints + inventory on cladding-self spec.yaml.
    evidence_refs:
      - spec.yaml
  - id: AC-005
    statement: The system shall use ISO date (YYYY-MM-DD) for last_synced to stay
      commit-stable.
