id: F-8f419e
title: Legacy deliverable smoke reads liveness, not green
status: done
modules:
  - src/stages/deliverable-smoke.ts
depends_on: []
capability_refs: []
acceptance_criteria:
  - id: AC-332ace
    statement: When the legacy project.deliverable runs clean (exit matches expect)
      with no AC-observable token, the smoke stage shall report disposition
      'liveness' — non-green and non-blocking — instead of a green pass, so an
      exit-only deliverable is never claimed as functionally verified.
