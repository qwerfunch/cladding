id: F-067
title: MISSING_TESTS severity promoted warn → error
status: done
modules:
  - src/stages/detectors/missing-tests.ts
  - tests/stages/missing-tests.test.ts
  - src/stages/detectors/README.md
depends_on:
  - F-052
  - F-064
capability_refs: []
acceptance_criteria:
  - id: AC-180
    statement: The system shall promote the MISSING_TESTS detector's default
      severity from warn to error, so a status=done AC without test_refs or
      evidence_refs fails `clad check` outright rather than only under
      `--strict`.
  - id: AC-181
    statement: While a status=done AC declares neither test_refs nor evidence_refs,
      the system shall emit a MISSING_TESTS finding with severity error in
      default mode (no `--strict` needed).
  - id: AC-182
    statement: The system shall update src/stages/detectors/README.md so the catalog
      row, status-policy table, and AC-evidence-taxonomy section all reflect the
      new error severity.
    evidence_refs:
      - src/stages/detectors/README.md
