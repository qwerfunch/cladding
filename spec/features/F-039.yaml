id: F-039
title: Intent Router — natural language to CLI verb
status: done
modules:
  - src/router/intent.ts
  - tests/router/intent.test.ts
depends_on: []
capability_refs: []
acceptance_criteria:
  - id: AC-059
    statement: When the intent router evaluates a prompt, the router shall return
      the first matching verb from the current ordered rule registry or unknown
      when no rule matches.
    kind: behavior
    notes: |
      ## Historical note
      The old five-verb list is retired: `work` and `drive` now map to `run`.
  - id: AC-060
    statement: When the router evaluates a prompt matching both a specific rule and
      the broad run rule, the router shall return the first specific rule's
      verb.
    kind: behavior
    notes: >
      ## Historical schema-0.1 note

      The retired work/drive/sync/check ordering is historical, not current
      proof.
