id: F-058
title: Detector unit tests batch 4 (final 2 + every detector at 100% line coverage)
status: done
modules:
  - tests/stages/hardcoded-secret.test.ts
  - tests/stages/architecture-violation.test.ts
depends_on: [F-057]
acceptance_criteria:
  - id: AC-137
    ears: ubiquitous
    action: cover HARDCODED_SECRET with focused unit tests
    response: tests/stages/hardcoded-secret.test.ts mocks execaSync via vi.mock('execa') to exercise every branch (no-gate / exit 0 / non-zero / ENOENT / non-ENOENT throw / stdout-only / no-output fallback); line coverage on the detector source reaches 100%
    text: The system shall ship tests/stages/hardcoded-secret.test.ts bringing the HARDCODED_SECRET detector to 100% line coverage by mocking execaSync.
    test_refs: [tests/stages/hardcoded-secret.test.ts]
  - id: AC-138
    ears: ubiquitous
    action: cover ARCHITECTURE_VIOLATION with focused unit tests
    response: tests/stages/architecture-violation.test.ts mirrors the HARDCODED_SECRET pattern (vi.mock('execa') + 7 branch tests); line coverage on the detector source reaches 100%
    text: The system shall ship tests/stages/architecture-violation.test.ts bringing the ARCHITECTURE_VIOLATION detector to 100% line coverage by the same mocking pattern.
    test_refs: [tests/stages/architecture-violation.test.ts]
  - id: AC-139
    ears: state
    condition: while current detector proof is evaluated
    action: derive it per criterion from live bindings and observations
    response: does not treat the former detector-test-file milestone as current proof
    text: While current detector proof is evaluated, it shall be derived per criterion from live bindings and observations; the former detector-test-file milestone is historical evidence only.
    test_refs:
      - "tests/proof/bindings.test.ts#[covers:F-058/AC-139] exact live bindings and observations determine criterion proof"
    notes: |
      ## Historical schema-0.1 note
      The 20-detector, dedicated-test, 100% milestone is historical, not current proof.
  - id: AC-140
    ears: state
    condition: while current coverage assurance is evaluated
    action: derive it from the coverage obligation
    response: does not treat the v0.2.9 measurement as current proof
    text: While current coverage assurance is evaluated, it shall be derived from the coverage obligation; the v0.2.9 measurement remains historical evidence only.
    test_refs:
      - "tests/assurance/registry.test.ts#[covers:F-055/AC-128][covers:F-056/AC-132][covers:F-057/AC-136][covers:F-058/AC-140][covers:F-059/AC-144][covers:F-060/AC-148][covers:F-061/AC-152][covers:F-062/AC-157][covers:F-063/AC-163][covers:F-064/AC-167] derives current coverage assurance only from the coverage obligation"
    notes: |
      ## Historical schema-0.1 note
      v0.2.9’s 58.66% to ≥62% measurement is historical, not current assurance.
