id: F-058
title: "Detector behavior evidence: secret scanning"
status: done
modules:
  - src/stages/detectors/hardcoded-secret.ts
  - tests/stages/hardcoded-secret.test.ts
  - tests/stages/architecture-violation.test.ts
depends_on:
  - F-057
capability_refs: []
acceptance_criteria:
  - id: AC-137
    statement: When HARDCODED_SECRET invokes its declared scanner adapter, the
      system shall derive findings from the current adapter output and error
      partition and classify its exact selector-matched same-run observation as
      verified, failed, or unobserved.
    notes: >
      ## Historical schema-0.1 note

      2026-05-19 · v0.2.9 · 1bcd501. HARDCODED_SECRET execa fixtures: no-gate,

      exit, ENOENT, thrown, stream, and empty output; 100% detector line
      coverage;

      non-current.

      Released title: "Detector unit tests batch 4 (final 2 + every detector at
      100% line coverage)".
  - id: AC-138
    statement: The system shall ship tests/stages/architecture-violation.test.ts
      bringing the ARCHITECTURE_VIOLATION detector to 100% line coverage by the
      same mocking pattern.
  - id: AC-139
    statement: While current detector proof is evaluated, the assurance registry
      shall derive it per criterion from live bindings and observations and
      treat the former detector-test-file milestone as historical evidence only.
    kind: quality
    notes: >
      ## Historical schema-0.1 note

      The 20-detector, dedicated-test, 100% milestone is historical, not current
      proof.
  - id: AC-140
    statement: While current coverage assurance is evaluated, the assurance registry
      shall derive it from the coverage obligation and treat the v0.2.9
      measurement as historical evidence only.
    kind: quality
    notes: |
      ## Historical schema-0.1 note
      v0.2.9’s 58.66% to ≥62% measurement is historical, not current assurance.
