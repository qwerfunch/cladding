id: F-040
title: clad CLI + Pulse UI + Integrity Panel
status: done
modules:
  - bin/clad
  - src/cli/clad.ts
  - src/ui/pulse.ts
  - src/ui/panel.ts
depends_on:
  - F-039
capability_refs: []
acceptance_criteria:
  - id: AC-061
    statement: The command registry shall expose only the current supported public
      commands and route each registered command through its declared handler;
      retired verb names remain compatibility history, not required commands.
    notes: >
      ## Historical schema-0.1 note

      The five Iron Core verbs plus panel/route list is historical, not current
      proof.
  - id: AC-062
    statement: The Pulse UI shall emit one line per transition (start / pass / fail
      / skip / note); output is ANSI-colored when stdout is a TTY and plain text
      otherwise.
    evidence_refs:
      - fixture:F-040_AC-062
  - id: AC-063
    statement: The Integrity Panel shall render a feature × stage text grid; each
      cell uses one of five glyphs (pass / skip / warn / error / unknown)
      describing the stage outcome for that feature.
    evidence_refs:
      - fixture:F-040_AC-063
