id: F-001
title: stage_1.1 Type — polyglot type-check delegation
status: done
modules:
  - src/stages/type.ts
  - src/stages/types.ts
  - src/stages/toolchain/detect.ts
  - src/stages/toolchain/types.ts
depends_on: []
capability_refs: []
acceptance_criteria:
  - id: AC-001
    statement: The system shall delegate type checking to the project's own
      toolchain, returning a stage_1.1 result whose exit code mirrors the
      underlying type checker.
    evidence_refs:
      - self-dogfood:stage:type
  - id: AC-002
    statement: When no type checker is registered for the detected language, the
      system shall return exitCode 2 so callers can treat the stage as skipped.
