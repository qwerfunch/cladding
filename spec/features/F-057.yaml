id: F-057
title: "Detector behavior evidence: audit logs and references"
status: done
modules:
  - src/stages/detectors/evidence-mismatch.ts
  - src/stages/detectors/stale-evidence.ts
  - src/stages/detectors/reference-integrity.ts
  - tests/stages/coverage-drop.test.ts
  - tests/stages/performance-drift.test.ts
  - tests/stages/evidence-mismatch.test.ts
  - tests/stages/stale-evidence.test.ts
  - tests/stages/reference-integrity.test.ts
depends_on:
  - F-056
capability_refs: []
acceptance_criteria:
  - id: AC-133
    statement: The system shall ship dedicated unit-test files for COVERAGE_DROP and
      PERFORMANCE_DRIFT, bringing each to 100% line coverage.
  - id: AC-134
    statement: When the audit-log detector adapters run, the system shall derive
      their declared audit-log and artifact inputs with their exact leading
      [covers:] selector closure and classify each same-run selector-matched
      case observation as verified, failed, or unobserved.
    notes: >
      ## Historical schema-0.1 note

      2026-05-19 · v0.2.8 · 286567d. EVIDENCE_MISMATCH/STALE_EVIDENCE audit-log

      fixtures: empty, present, missing-artifact, and old-timestamp cases; 100%

      line coverage for both sources; non-current. Released title: "Detector
      unit tests batch 3

      (5 detectors, 100% line coverage each)".
  - id: AC-135
    statement: When REFERENCE_INTEGRITY evaluates the compiled reference graph, the
      system shall resolve declared dependency, supersession, and scenario edges
      against the current feature graph and classify each exact selector-matched
      current observation as verified, failed, or unobserved.
    notes: >
      ## Historical schema-0.1 note

      2026-05-19 · v0.2.8 · 286567d. REFERENCE_INTEGRITY depends_on,
      superseded_by,

      and scenarios.features fixtures: 100% detector line coverage; non-current.
  - id: AC-136
    statement: While current coverage assurance is evaluated, the assurance registry
      shall derive it from the coverage obligation and treat the v0.2.8
      measurement as historical evidence only.
    kind: quality
    notes: |
      ## Historical schema-0.1 note
      v0.2.8’s 49% to ≥55% measurement is historical, not current assurance.
