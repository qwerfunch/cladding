id: F-060
title: Stage runner tests batch 2 (5 stages)
status: done
modules:
  - tests/stages/arch.test.ts
  - tests/stages/secret.test.ts
  - tests/stages/unit.test.ts
  - tests/stages/cov.test.ts
  - tests/stages/smoke.test.ts
depends_on: [F-059]
acceptance_criteria:
  - id: AC-145
    ears: ubiquitous
    action: cover the two detector-adapter stages
    response: tests/stages/arch.test.ts and tests/stages/secret.test.ts each mock the underlying detector (architectureViolation / hardcodedSecret) and exercise no-findings / info-only / single-error / multi-error / mixed-severity / opts-forwarding paths; line coverage on both stage sources reaches at least 75%
    text: The system shall ship dedicated unit-test files for src/stages/arch.ts and src/stages/secret.ts, bringing each to at least 75% line coverage by mocking the underlying detector.
    test_refs:
      - tests/stages/arch.test.ts
      - tests/stages/secret.test.ts
  - id: AC-146
    ears: ubiquitous
    action: cover the unit and coverage stage runners
    response: tests/stages/unit.test.ts and tests/stages/cov.test.ts each exercise unknown-language / explicit override / exit 0 / non-zero / stderr-present / stderr-absent / null-exit branches with vi.mock('execa'); line coverage on both stage sources reaches at least 85%
    text: The system shall ship dedicated unit-test files for src/stages/unit.ts and src/stages/cov.ts, bringing each to at least 85% line coverage.
    test_refs:
      - tests/stages/unit.test.ts
      - tests/stages/cov.test.ts
  - id: AC-147
    ears: ubiquitous
    action: cover the smoke stage (richer branch tree)
    response: tests/stages/smoke.test.ts exercises unknown / npm-script-missing / npm-script-present-exit-0 / non-zero / ENOENT / non-ENOENT throw / null-exit / explicit-override paths; line coverage reaches at least 85%
    text: The system shall ship tests/stages/smoke.test.ts bringing the stage_3.1 smoke runner to at least 85% line coverage.
    test_refs: [tests/stages/smoke.test.ts]
  - id: AC-148
    ears: state
    condition: while current coverage assurance is evaluated
    action: derive it from the coverage obligation
    response: does not treat the v0.2.11 measurement as current proof
    text: While current coverage assurance is evaluated, it shall be derived from the coverage obligation; the v0.2.11 measurement remains historical evidence only.
    test_refs:
      - "tests/assurance/registry.test.ts#[covers:F-055/AC-128][covers:F-056/AC-132][covers:F-057/AC-136][covers:F-058/AC-140][covers:F-059/AC-144][covers:F-060/AC-148][covers:F-061/AC-152][covers:F-062/AC-157][covers:F-063/AC-163][covers:F-064/AC-167] derives current coverage assurance only from the coverage obligation"
    notes: |
      ## Historical schema-0.1 note
      v0.2.11’s 72.58% to ≥80% measurement is historical, not current assurance.
