id: F-aa7197
title: scan audit P1 deterministic fix — cwd resolve + forbidden prune +
  non-source blacklist (v0.3.31)
status: done
modules:
  - src/cli/scan/architecture.ts
  - src/cli/scan/thresholds.ts
depends_on:
  - F-cfba0c
capability_refs: []
acceptance_criteria:
  - id: AC-001
    statement: When root-bucket promotion names a layer, the system shall use the
      resolved workspace basename without emitting `.` as a layer name.
    kind: behavior
    notes: >
      ## Historical note

      The cobra/I15 and corpus byte-stability language is a dated audit
      observation, not the invariant.
  - id: AC-002
    statement: The system shall prune the forbidden_imports candidate matrix to a
      manageable size by excluding trivial layers and capping per-entry width so
      the 5차 audit noise (ripgrep 195 + vitest 380+ entries) no longer reaches
      authoritative YAML.
  - id: AC-003
    statement: The system shall expand LAYER_BLACKLIST to cover packaging and
      documentation source directories surfacing across the audit corpus so
      HomebrewFormula and docs_src no longer ship as authoritative architecture
      layers.
  - id: AC-004
    statement: The system shall keep cladding self-scan plus the 16-OSS corpus
      regression-stable so the v0.3.30 audit baseline holds while the residual
      fixes land.
    evidence_refs:
      - .cladding/audit/scan-real-world-2026-05-20.md
