# Cladding · Tier A · SSoT — Iron Law sealed · Refreshed by: manual
id: F-0a29d024
slug: spec-02-design-validation
title: "Spec 0.2 design validation — executable scenarios, MCP efficacy, and token accounting"
status: done
modules:
  - scripts/spec-0.2-validate.ts
  - tests/design/spec-0.2/requirements.yaml
  - tests/design/spec-0.2/design-validation.test.ts
  - tests/design/spec-0.2/mcp-validation.test.ts
  - docs/design/spec-0.2/validation.md
  - docs/design/spec-0.2/mcp.md
  - docs/design/spec-0.2.md
  - docs/design/spec-0.2/delivery.md
  - docs/design/spec-0.2/decision-log.md
  - docs/README.md
  - package.json
acceptance_criteria:
  - id: AC-21dfe21a
    ears: ubiquitous
    text: "The validation ledger shall map every D01-D24 decision to a canonical owner, an executable or explicitly deferred scenario, and a unique evidence classification without presenting preregistered work as a passing result."
    test_refs: ["tests/design/spec-0.2/design-validation.test.ts#maps every D01-D24 decision exactly once and rejects fabricated pass evidence"]
    notes: "## Why\nThe accepted design is larger than the 37 parser/migration seeds, so section-level prose confidence is not enough to begin a long implementation safely."
  - id: AC-e0f0c4b1
    ears: ubiquitous
    text: "The deterministic simulator shall evaluate design ownership, migration and proof contracts, GraphIR and context constraints, assurance scheduling, and delivery ordering with independent fixtures while leaving implementation-pending behavior explicitly unverified."
    test_refs: ["tests/design/spec-0.2/design-validation.test.ts#runs deterministic design scenarios without converting pending implementation into evidence"]
  - id: AC-bd12a73c
    ears: ubiquitous
    text: "The MCP validation suite shall distinguish wire conformance, semantic parity, mutation safety, reference-host end-to-end use, efficiency when used, and observed adoption, and shall fail closed when any required evidence is absent."
    test_refs: ["tests/design/spec-0.2/mcp-validation.test.ts#separates MCP conformance efficacy and adoption without vacuous success"]
  - id: AC-4c8c08d1
    ears: ubiquitous
    text: "Every deterministic and live validation result shall report exact controlled UTF-8 bytes, a named token estimator, cache knowledge, and only counterfactually demonstrated avoidable bytes as waste."
    test_refs: ["tests/design/spec-0.2/design-validation.test.ts#accounts for controlled bytes tokens cache and counterfactual waste honestly"]
  - id: AC-55c159b4
    ears: event
    condition: "when the Spec 0.2 validation command runs"
    text: "When the Spec 0.2 validation command runs, the system shall emit a deterministic machine-readable summary and a human-readable report whose pass, fail, inconclusive, not-run, and implementation-pending states remain distinct."
    test_refs: ["tests/design/spec-0.2/design-validation.test.ts#emits byte-identical summaries with distinct result states"]
design_impact:
  classification: additive
  rationale: "The work adds a contributor-only validation boundary and target-design owner without changing the shipped 0.1 runtime contract."
  status: resolved
  artifacts: []
