id: F-041
title: Token Optimizer — pruning, preamble suppression, tail-only logging
status: done
modules:
  - src/optimizer/prune.ts
  - src/optimizer/preamble.ts
  - src/optimizer/tail.ts
depends_on: []
capability_refs: []
acceptance_criteria:
  - id: AC-064
    statement: The system shall prune a Spec to one feature plus its transitive
      depends_on, including any scenario that mentions a retained feature.
      Original spec is returned unchanged when featureId is unknown.
  - id: AC-065
    statement: "The system shall strip persona preamble lines (You are X, #
      Orchestrator/Librarian/..., Your job is...) from a prompt and collapse the
      resulting whitespace."
    evidence_refs:
      - fixture:F-041_AC-065
  - id: AC-066
    statement: The system shall shrink long text to head N + tail M lines with an
      elision marker, returning the original text unchanged when total lines fit
      within head + tail.
    evidence_refs:
      - fixture:F-041_AC-066
