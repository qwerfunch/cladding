id: F-041
title: Token Optimizer — pruning, preamble suppression, tail-only logging
status: done
modules:
  - src/optimizer/prune.ts
  - src/optimizer/envelope.ts
depends_on: []
capability_refs: []
acceptance_criteria:
  - id: AC-064
    statement: The system shall prune a Spec to one feature plus its transitive
      depends_on, including any scenario that mentions a retained feature.
      Original spec is returned unchanged when featureId is unknown.
  - id: AC-065
    kind: behavior
    statement: The context envelope shall strip persona preamble lines from
      diagnostic sections before measurement and collapse the resulting blank
      runs.
    evidence_refs:
      - fixture:F-041_AC-065
  - id: AC-066
    kind: behavior
    statement: When a diagnostic section exceeds its head and tail allowance, the
      context envelope shall keep the head and tail lines with an elision marker
      that names the exact number of elided lines.
    evidence_refs:
      - fixture:F-041_AC-066
